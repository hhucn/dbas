version: '2'
services:
  db:
    build: database/
    ports:
      - "5433:5432"
  web:
    build: .
    command: /bin/bash docker_init.sh
    volumes:
      - .:/code
    ports:
      - "4284:4284"
    links:
      - db:db