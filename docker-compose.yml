version: "3.1"
services:
  db:
    image: hhucn/dbas-database
    ports:
      - "5432:5432"
    tmpfs: /tmp

  web:
    build: .
    command: bash -c "sleep 8 && python setup.py develop && pserve development.ini --reload"
    volumes:
      - .:/dbas
    ports:
      - "4284:4284"
    links:
      - db:db
    env_file:
      - development.env
    depends_on:
      - db
    tmpfs: /tmp

  notifications:
    image: hhucn/dbas-notifications
    ports:
      - "5222:5222"
    tmpfs: /tmp
